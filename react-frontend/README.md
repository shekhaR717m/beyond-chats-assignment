# react-frontend

Phase 3: React + Vite frontend

Setup:

- Run `npm install` inside this folder to install dependencies.
- Use `npm run dev` to start the dev server.

**Note:** Dependencies were installed successfully during setup (`npm install`). Run `npm audit` to review recommended fixes.
